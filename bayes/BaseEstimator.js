class BaseEstimator {
    constructor(object){

    }

    _get_param_names(cls) {
        /**
         * Get parameter names for the estimator
         */
        //init = 
    // not finished
    }

    get_params(deep = true) {
        out = {};
        key = this._get_param_names()
        for(let i = 0; i < key.lenght ;i++) {
            let value = self.key[i]

        }

    // not finished
    }

    set_params() {
    // not finished
    }

    __repr__() {
    // not finished
    }

    __getstate__() {
    // not finished
    }

    __setstate__() {
    // not finished
    }

}
module.exports = BaseEstimator;